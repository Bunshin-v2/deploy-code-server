interface:
  display_name: "Deploy Code Server"
  short_description: "Repo-specific patterns and workflows for deploy-code-server"
  default_prompt: "Use the deploy-code-server repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true